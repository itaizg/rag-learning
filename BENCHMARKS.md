# P5 Optimized Inference Service -- Benchmark Report

_Auto-generated by `06_projects/P5_optimized_inference_service.ipynb` on 2026-07-30._

## Methodology
- Fixed workload (identical across every layer and trial): a Poisson arrival process over 25 interleaved multi-turn conversations, so caching and admission-control effects are measured against the SAME traffic, not resampled per layer.
- 3 trials per layer; every reported number is the **median** across trials (only the decode-time jitter RNG seed varies between trials -- the workload itself never changes).
- Backend: an in-process `asyncio` timing model (bounded semaphore for GPU slots, simulated prefill + per-token decode delay) -- NOT a real model or a real GPU. See Limitations.

## Configuration
| Parameter | Value |
|---|---|
| BATCH_SLOTS | 16 |
| PROMPT_TOKENS | 500 |
| NEW_TURN_TOKENS | 50 |
| N_TOKENS (output) | 8 |
| N_CONVERSATIONS | 25 |
| ARRIVAL_RATE (req/s offered) | 190.0 |
| DURATION_S | 4.0 |
| SHED_THRESHOLD | 40 |
| N_TRIALS | 3 |
| workload size | 725 |

## Results (median of 3 trials, 725 requests/trial)
| Layer | Throughput (req/s) | P50 (ms) | P99 (ms) | Shed | Cost (USD) | Avg. quality |
|---|---:|---:|---:|---:|---:|---:|
| L0 baseline | 119.2 | 990.3 | 2060.5 | 0 | $1.1745 | 1.000 |
| L1 +prefix caching | 147.6 | 407.5 | 901.7 | 0 | $1.1745 | 1.000 |
| L2 +admission control | 146.1 | 239.7 | 280.9 | 104 | $1.0060 | 1.000 |
| L3 +quantized model swap | 179.0 | 66.7 | 96.8 | 0 | $0.3915 | 0.625 |
| L4 +cost/quality routing | 177.1 | 81.4 | 140.4 | 0 | $0.6118 | 0.731 |

## Honest limitations
- The backend simulates latency with `asyncio.sleep`, not a real model doing real token generation -- absolute numbers are illustrative of the SHAPE of each optimization's effect, not a claim about any specific real model or GPU.
- "Quality" is a fixed constant per model tier (reused from notebook 32c's own small golden-set measurement), not re-evaluated per request here -- a real deployment needs its own eval, per 32c's small-golden-set-noise finding.
- Measured on a shared, multi-tenant sandbox CPU -- wall-clock numbers carry more run-to-run noise than they would on dedicated hardware, which is exactly why this report uses medians over 3 trials rather than a single run.
- The workload (25 conversations, one fixed arrival rate) is illustrative, not a real production traffic trace -- the qualitative story (caching helps partially, admission control protects the tail, quantization fixes root capacity, routing buys back quality) is the transferable lesson, not the specific millisecond values.
